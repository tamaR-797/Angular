import { HighlightInvalidDirective } from './highlight-invalid.directive';

describe('HighlightInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
